package views;

import display.View;
import game.Settings;

public class ViewSettings extends View {

    public ViewSettings(View parrent,Settings settings) {
        super(parrent,settings);
    }
    
}
